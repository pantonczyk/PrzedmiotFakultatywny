import React from 'react';
import MainTemplate from '../../templates/MainTemplate';

const Notes = () => {
   return (
      <MainTemplate>
         <h1>Notes View</h1>
      </MainTemplate>
   );
};

export default Notes;
